{
  "name": "bruno_pesenti",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); if (fs.existsSync('dist')) { fs.rmSync('dist', { recursive: true, force: true, maxRetries: 3, retryDelay: 100 }); }\"",
    "deploy": "npm run clean && vite build && gh-pages -d dist"
  },
  "dependencies": {
    "cloudinary": "^2.9.0",
    "emailjs-com": "^3.2.0",
    "mongodb": "^7.0.0",
    "multer": "^2.0.2",
    "vue": "^3.5.13",
    "vue-i18n": "^11.1.1",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "gh-pages": "^6.3.0",
    "vite": "^6.1.0",
    "vite-plugin-vue-devtools": "^7.7.2"
  }
}
